plugins {
    id "java"
    id "application"
}

mainClassName = 'random.Main'

jar {
    baseName = 'transfer'

    manifest {
        attributes(
                'Class-Path': '',
                'Main-Class': 'random.Main'
        )
    }

}